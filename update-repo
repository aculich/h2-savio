#!/bin/bash

# Pull from remote and force overwrite any local changes
# The -X theirs strategy option tells git to automatically choose the remote version for any conflicts
git pull -X theirs https://github.com/aculich/h2-savio/
git reset --hard HEAD
